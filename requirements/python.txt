# Controller-side libraries for Linux, Windows, APIs, Git, VMware, containers,
# Kubernetes, databases, DNS, Vault, parsing, and playbook validation.
ansible-lint==25.1.3
bcrypt==4.2.1
# impacket 0.12.0 pins pyOpenSSL 24.0.0, which requires cryptography <43.
cryptography==42.0.8
dnspython==2.7.0
docker==7.1.0
gitpython==3.1.44
hvac==2.3.0
# Provides psexec.py for bootstrapping WinRM over SMB when explicitly required.
impacket==0.12.0
jmespath==1.0.1
jsonschema==4.23.0
kubernetes==32.0.1
netaddr==1.3.0
passlib==1.7.4
psycopg[binary]==3.2.6
pymysql==1.1.1
pypsrp==0.8.1
pyvmomi==8.0.3.0.1
# NTLM and CredSSP cover password-based WinRM without compiling legacy
# pykerberos. Kerberos can be added later as a separate, tested profile.
pywinrm[credssp]==0.5.0
requests==2.32.3
requests-credssp==2.0.0
requests-ntlm==1.3.0
yamllint==1.35.1
